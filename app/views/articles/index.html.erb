<h1>Fresh Articles</h1>

<ul class = "art_list">
	<% @articles.each do |article| %>
	<li><%= link_to article.title, article_path(article), class: 'article_title' %></li><br/>
	<% end %>
</ul>
<%= link_to 'New Article', new_article_path, class: 'new_article' %>